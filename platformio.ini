[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
upload_speed = 115200
lib_deps =
    fastled/FastLED

; Optional: makes warnings easier to catch while debugging.
build_flags =
    -Wall
    -Wextra
